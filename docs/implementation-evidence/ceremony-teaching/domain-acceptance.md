# Domain acceptance evidence: AC-05–AC-38

This is an assertion inventory, not a production certification or a commit-bound PASS artifact. The coordinating release runner must bind actual results to its final checkout/configuration. A test title containing an acceptance ID does not prove every requirement of that case. Browser/platform results must come from their own runners, not from this domain coverage run.

## Executed results

The coordinating storage/domain agent executed three full `npm run test:coverage` attempts during integration:

1. Failed: missing Chromium's default-cache executable, a cancelled-callback HTTP status regression, and 89.97% global function coverage. The original console capture was truncated; an exact named-test count was not retained and is not reconstructed here.
2. Failed: **237 tests, 235 passed, 2 failed, 0 skipped**. Failures were `tests/cloudflare.test.ts` (missing executable) and `tests/teaching-http.test.ts` (cancelled callback returned 400 instead of the required 403). Aggregate coverage was 90.43% lines, 88.40% branches, 90.21% functions.
3. Passed: **245 tests, 245 passed, 0 failed/skipped/cancelled**. Correct installed-browser configuration exercised real Chromium; the callback policy error was normalized to an explicit authorization denial. Aggregate coverage was **92.89% lines/statements, 90.22% branches, 90.49% functions**. The result was stored at `artifacts/coverage/coverage-summary.json`; later coordinated runs may replace that file.

The passing invocation used `NODE_OPTIONS=--no-experimental-webstorage`, `PLAYWRIGHT_BROWSERS_PATH=/tmp/ceremony-browsers`, `LD_LIBRARY_PATH=/tmp/ceremony-browser-libs-h6vXYL/root/usr/lib/aarch64-linux-gnu`, and `PLAYWRIGHT_WEBKIT_EXECUTABLE_PATH=/tmp/ceremony-webkit-run.sh`. These are this worker's installed runtime paths, not portable production configuration. The Node discovery runner includes nested contract/security tests and deliberately runs Workflow tests separately under their supported runtime. It does not run the Playwright browser matrix.

Subsequent focused, non-excluding runs:

| Scope                                                                         | Actual result                                 | Changed-module branch coverage                | Report location                            |
| ----------------------------------------------------------------------------- | --------------------------------------------- | --------------------------------------------- | ------------------------------------------ |
| GitHub children/runtime and mounted HTTP                                      | 16/16 passed                                  | GitHub children 91.01%; GitHub runtime 92.78% | `artifacts/github-branch-coverage/`        |
| Projection/wake/host continuation                                             | 11/11 passed                                  | Projections 94.28%; Workflow API 94.87%       | `/tmp/ceremony-projection-wake-coverage/`  |
| Runtime admission/delegation/continuation plus existing authority regressions | 16/16 passed; final new-test rerun 3/3 passed | Teaching runtime 91.78%                       | `/tmp/ceremony-runtime-coverage/`          |
| Storage maintenance and existing persistence                                  | 10/10 passed at that stage                    | Maintenance 100%; persistence 96.80%          | `artifacts/teaching-maintenance-coverage/` |
| Session Environment and legacy compatibility                                  | 8/8 passed                                    | Async Environment 96.82%                      | `artifacts/teaching-environment-coverage/` |

Focused reports are not interchangeable with full-suite coverage. Later tests added atomic rotation page checks and other boundaries; do not add overlapping counts together. The final checked TypeScript invocation passed after the runtime fixture's classification contract was corrected. No production branch was excluded or coverage threshold lowered for these results.

Additional AC-27/29/33 closure: `tests/command-crash-boundaries.test.ts` adds one concurrent UI/agent PostgreSQL scenario, four real worker-process termination scenarios, and six systematically enumerated callback/cancel/worker orderings. An intermediate 20-test run had 17 passes and three failing assertions because it expected detailed demonstration events without first recording consent; the fixture was corrected to explicitly start teaching. The corrected 20-test run passed. The subsequent combined command/security/HTTP coverage run passed **25/25**, with `commands.ts` at **99.61% lines, 91.20% branches, 100% functions** (`/tmp/ceremony-crash-coverage/`). Production fixes from these tests fence expired running commands into explicit uncertainty, prevent new command IDs from repeating uncertain effects, and allow only the signed, purpose-bound GitHub recovery path to restore verification eligibility atomically.

## Assertion mapping and limits

Paths below are relative to the repository. “Partial” identifies an essential local assertion or combined proof not established by this agent's inspected/executed evidence; it must not be silently mapped to PASS by the aggregator.

| Case  | Existing assertion source                                                                                                                     | What is demonstrated; remaining proof boundary                                                                                                                                                                                                                                                                                                                                                                       |
| ----- | --------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AC-05 | `tests/recipes.test.ts`; `tests/browser/teaching-acceptance.spec.ts`                                                                          | Draft/review/published reuse behaviors exist. **Unmapped:** no dedicated AC-05 assertion was found; the supplied brief's early acceptance rows were incomplete. Resolve its exact scenario before assigning a verdict.                                                                                                                                                                                               |
| AC-06 | Same authoring/browser suites                                                                                                                 | Whole/selected demonstrations are exercised. **Unmapped:** no dedicated AC-06 assertion was found; do not invent its expected scenario or mark it passed.                                                                                                                                                                                                                                                            |
| AC-07 | `tests/commands.test.ts`; `tests/connection-tools.test.ts`                                                                                    | Authoritative recording does not depend on browser callbacks; abort fences late tool delivery. Browser Stop/Cancel/Discard interaction proof belongs to the UI runner.                                                                                                                                                                                                                                               |
| AC-08 | `tests/security/automatic-composition.test.ts`; `tests/security/recipe-resources.test.ts`                                                     | Independently authored published fragments compose by typed dependency; a fresh executor gets fresh principal-bound artifacts and zero model calls.                                                                                                                                                                                                                                                                  |
| AC-09 | `tests/recipes.test.ts`; `tests/security/recipe-validation.test.ts`                                                                           | Selected fragments require fresh external inputs, reject noncontiguous selections, and preserve failures as diagnostics rather than inferred success.                                                                                                                                                                                                                                                                |
| AC-10 | `tests/security/demonstration-retention.test.ts`; `tests/security/teaching-route-lifecycle.test.ts`; browser teaching acceptance              | Consent revisions and callback capture boundaries, pagination beyond 1,000 records, discard and retained audit state. Browser race evidence is separate.                                                                                                                                                                                                                                                             |
| AC-11 | `tests/teaching-contracts.test.ts`; `tests/security/recipe-validation.test.ts`; `tests/security/teaching-http.test.ts`                        | Imported authority/extra keys/unsafe definitions rejected; import creates drafts; export excludes authority and private provenance.                                                                                                                                                                                                                                                                                  |
| AC-12 | `tests/recipes.test.ts`; `tests/security/automatic-composition.test.ts`                                                                       | Reviewed revision/digest binding, pinned child versions, latest-selection ambiguity and current retirement policy.                                                                                                                                                                                                                                                                                                   |
| AC-13 | `tests/security/automatic-composition.test.ts`; `tests/teaching-http.test.ts`                                                                 | Unsupported connector/profile and ambiguous capabilities do not select invented execution. Browser/tool capability presentation must be checked separately.                                                                                                                                                                                                                                                          |
| AC-14 | `tests/teaching-http.test.ts`; `tests/security/automatic-composition.test.ts`; `tests/security/agent-status.test.ts`                          | Deterministic GitHub children and published composition execute without a model; waiting snapshots consume no inference. Browser publication/replay is separately exercised.                                                                                                                                                                                                                                         |
| AC-15 | `tests/connection-tools.test.ts`; `tests/webmcp-unit.test.ts`; `tests/agent.test.ts`                                                          | Local/native-adapter schemas and protected command decisions match; actual AI SDK HTTP tool transport runs. Native browser registration is not certified by adapter mocks.                                                                                                                                                                                                                                           |
| AC-16 | `tests/security/command-bindings.test.ts`; `tests/commands.test.ts`; `tests/teaching-runtime-boundaries.test.ts`                              | Wrong-state fields, private literals, command substitution, unmet prerequisites and invalid/unknown public arguments rejected before effects/run admission.                                                                                                                                                                                                                                                          |
| AC-17 | `tests/agent.test.ts`; `tests/security/recipe-validation.test.ts`; `tests/security/authoring-isolation.test.ts`                               | Actual AI SDK HTTP rejects unknown tools and scope/verifier/source smuggling; malicious author descriptions never enter the authoring request, executable suggestions exhaust the two-call repair limit without publication, and registry/projection allowlists exclude arbitrary provider copy. No narration or attachment ingress is exposed.                                                                      |
| AC-18 | `tests/security/teaching-authority.test.ts`; `tests/teaching-runtime-boundaries.test.ts`; `tests/identity.test.ts`                            | Revoked/expired/wrong-session delegation, Stop during an effect, effect binding, and revision change during awaited host authorization deny continuation.                                                                                                                                                                                                                                                            |
| AC-19 | Identity, hosted HTTP, security teaching HTTP and runtime-boundary suites                                                                     | Foreign subject/tenant requests, publication rights, streams, private collectors, continuation ownership and callback recipient checks. Signed OIDC uses real local HTTP.                                                                                                                                                                                                                                            |
| AC-20 | Collection/Environment/MCP recipient tests; `tests/security/authoring-isolation.test.ts`; `tests/browser/environment.spec.ts`                 | Synthetic canaries reach the authorized collector/Environment transport and encrypted store, but not actual AI SDK requests, safe run/demo/outbox/stream projections, browser console/error output, storage or caches. This is the configured application path, not certification of a future third-party analytics processor.                                                                                       |
| AC-21 | `tests/teaching-contracts.test.ts`; `tests/webmcp-unit.test.ts`; `tests/connection-tools.test.ts`                                             | Device codes/URLs/references remain only in allowed human projections; absent fields stay absent; agent errors and unknown fields cannot inherit them.                                                                                                                                                                                                                                                               |
| AC-22 | `tests/teaching-contracts.test.ts`; `tests/async-environment.test.ts`                                                                         | Legacy unclassified text is not public; password/token fields cannot downgrade classification; operation policy rejects invalid public values.                                                                                                                                                                                                                                                                       |
| AC-23 | `tests/agent.test.ts`; `tests/security/agent-identifiers.test.ts`                                                                             | Known secret formats rejected before actual model HTTP ingress. Arbitrary unknown passwords cannot be perfectly recognized; no universal detection claim.                                                                                                                                                                                                                                                            |
| AC-24 | Private recovery/collector tests; `tests/browser/environment.spec.ts`; `tests/browser/diagnostics.spec.ts`; PWA and authoring-isolation tests | Complementary actual browser/server instrumentation checks private form cleanup, app request/console/storage/cache sinks and model/recording exclusions. An intentionally failing Playwright child proves the installed runner emits no DOM/ARIA snapshot, screenshot, video, HAR or trace attachment. No third-party replay/analytics SDK is configured; adding one requires new exclusion tests before enablement. |
| AC-25 | Projection/schema tests; `tests/security/command-bindings.test.ts`; `tests/security/agent-identifiers.test.ts`                                | Extra JSON/authority fields, private structural values, secret-shaped labels and unknown tool values fail before trusted execution/model egress.                                                                                                                                                                                                                                                                     |
| AC-26 | Contract/recipe resource tests; agent tests; GitHub child tests                                                                               | Exact import limits, graph expansion/depth, unsupported bindings, persistent model budgets and 32-subscriber shared setup ceiling are asserted.                                                                                                                                                                                                                                                                      |
| AC-27 | `tests/command-crash-boundaries.test.ts`; persistence/collection/GitHub suites                                                                | Concurrent authenticated UI and agent HTTP requests use distinct PostgreSQL connections and actual durable delegation rehydration. The real HTTP provider ledger records one effect; duplicate callers converge on the same verified command.                                                                                                                                                                        |
| AC-28 | `tests/persistence.test.ts`; `tests/security/fencing-generation.test.ts`; `tests/agent-wake-boundaries.test.ts`                               | Independent workers, expired/taken-over generations, same-workload restart and busy wake claims cannot commit stale work.                                                                                                                                                                                                                                                                                            |
| AC-29 | `tests/command-crash-boundaries.test.ts`; `tests/fixtures/command-crash-worker.ts`; chaos/GitHub suites                                       | Actual isolated processes terminate before request, after provider effect with lost response, after response before commit, and after commit before acknowledgment. Expired running commands become fenced uncertainty with atomic teaching events/outbox; neither same nor new command identity repeats uncertain effects. Committed effects replay verified outcomes.                                              |
| AC-30 | GitHub child, mounted teaching HTTP and browser recovery suites                                                                               | One-use manifest conversion response loss enters uncertainty; private app recovery verifies the existing app and avoids another conversion.                                                                                                                                                                                                                                                                          |
| AC-31 | `tests/persistence.test.ts`; `tests/commands.test.ts`                                                                                         | Awaited transaction rejection rolls back writes/events/outbox together; duplicate continuation delivery retains identity.                                                                                                                                                                                                                                                                                            |
| AC-32 | `tests/github-children.test.ts`; `tests/github-runtime-http.test.ts`                                                                          | Independent PostgreSQL parents share full-context setup; cancelling one leaves another; wrong principal cannot join; cancelled-original callback reauthorizes a survivor; replay denied. Pre-handoff cancel-all permits fresh setup; issued/uncertain effects require reconciliation.                                                                                                                                |
| AC-33 | `tests/command-crash-boundaries.test.ts`; GitHub children and authority suites                                                                | All six callback/cancel/worker-result orderings run through the command service and real HTTP provider. A callback alone cannot verify; cancellation fences late results; already verified upstream effects are not represented as revoked. Separate GitHub/identity tests reject expired, replayed and wrong-recipient handoffs.                                                                                    |
| AC-34 | `tests/workflow/agent.test.ts`; `tests/agent-wake-boundaries.test.ts`; `tests/security/agent-status.test.ts`                                  | Actual supported local Workflow runtime waits/resumes using shared PostgreSQL; safe wake outbox retries, deletion races and session budgets are covered. Workflow tests are separate from the 245-test Node run. Hosted-platform durability remains an external gate.                                                                                                                                                |
| AC-35 | `tests/hosted-continuations.test.ts`; `tests/teaching-runtime-boundaries.test.ts`; browser continuation test                                  | Actual HTTP consumer applies one logical effect despite lost acknowledgment; wrong/missing/unfinished/retargeted/currently denied host tasks cannot run. Closed-tab proof comes from the browser runner.                                                                                                                                                                                                             |
| AC-36 | Authority, async Environment, GitHub child/runtime tests                                                                                      | Fresh provider verification rejects revoked evidence; dependent artifacts invalidate; only relevant GitHub variable changes rotate config; stale callback/session authority is rejected.                                                                                                                                                                                                                             |
| AC-37 | Demonstration retention and persistence maintenance tests                                                                                     | Teaching events/pointers deleted, newer pointers and published/audit records preserved, mismatched ownership rolls back deletion. Browser discard behavior is separate.                                                                                                                                                                                                                                              |
| AC-38 | Persistence and maintenance tests                                                                                                             | Real PostgreSQL encrypted source-to-empty-target restore, wrong-key/tamper/duplicate rejection, restored worker fencing and current-key rotation; old-key retirement requires operator backup policy.                                                                                                                                                                                                                |

## Certification boundary

This agent did not execute real-account GitHub certification, a deployed Vercel/Workflow durability check, or installed-PWA device certification. Local signed GitHub/OIDC/model fixtures and local PostgreSQL are deterministic evidence, not substitutes for those gates. Missing local assertions above are engineering/evidence gaps, not unavailable external credentials. This document cannot override the machine-derived release verdict.
