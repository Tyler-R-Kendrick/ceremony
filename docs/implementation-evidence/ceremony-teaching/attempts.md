# Clean-checkout verification attempts

The broad legacy mutation job in Actions run `34397964266` was cancelled at its 30-minute job limit. Its log recorded 1,037 mutants across seven production modules and a passing initial run of 48 TAP files in 54 seconds; it contained no completed mutation result. This is failed verification, not a passing timeout. CI now partitions the same seven complete targets into independent existing Ubuntu runner jobs with two workers each, unchanged test selection and unchanged mutation thresholds. Each target retains its own result artifact. The installed Stryker CLI documents `--mutate` as the target override; no source range, mutant type, or test is excluded. The matrix changes scheduling only and does not establish a passing result until all seven jobs finish successfully.

These failures are retained even after corrective commits. They are not passing retries or external certification.

| Commit                                     | Environment                       | Observed result                                                                                                                                                             | Disposition                                                                                                                                                                                     |
| ------------------------------------------ | --------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `b8cee2e280d7a4681ae5281702f1fb2510b1baa9` | Local, `2026-09-09T19-55-13-511Z` | Node coverage: 252 passed, 16 failed. Native PostgreSQL libraries were not hydrated because npm lifecycle scripts were disabled.                                            | Reviewed package-local hydration restored the native installation; affected ten files subsequently passed 52 tests. The original complete attempt remains failed.                               |
| `b8cee2e280d7a4681ae5281702f1fb2510b1baa9` | GitHub Actions run `34397964266`  | 268 Node tests, two Workflow tests, 27 security mutants and both builds passed. Browser matrix: 64 passed, one failed.                                                      | The old artifact policy retained counts but not the failed source location. Its exact failing assertion is unavailable; no cause is inferred. A positive-allowlist location reporter was added. |
| `1480fb0936b324bc02c5802fe4819fbfd31f6b82` | Local, `2026-09-09T20-14-57-964Z` | Formatting, TypeScript, 276 Node tests, two Workflow tests, 41 killed security mutants and both builds passed. Browser matrix: 64 passed, one failed, zero skipped/retries. | Safe reporter identified Firefox `tests/browser/teaching-accessibility.spec.ts:21`. This attempt is failed, pending a diagnosed correction and new commit.                                      |
| `1480fb0936b324bc02c5802fe4819fbfd31f6b82` | GitHub Actions run `34399871008`  | 276 Node tests passed. Workflow: one passed, one failed; subsequent verification stages did not run.                                                                        | The existing aggregate did not retain the failed assertion location. This is a failed attempt, not proof of a particular timing defect.                                                         |

Earlier integration and focused attempts are recorded in [domain acceptance](domain-acceptance.md) and [browser acceptance](acceptance-browser.md). Counts from overlapping runs must not be added together. Local artifacts contain only allowed counters, source locations and configuration/runtime metadata; no provider captures, private DOM or raw model histories are retained.

GitHub Actions run `34404876337`, commit `5ebb6967b09382503d8e5044c59e8ac82eda53d4`, failed deterministic verification on Node `24.20.0`: 283 Node tests, two Workflow tests, 62 security mutants, formatting, type checking and both builds passed; browsers recorded 67 passed and one failed, with no retries or skips. The sanitized artifact identifies Chromium `tests/browser/environment.spec.ts:4` (test declaration), duration 5,326 ms, but does not retain its failed assertion. This remains a failed attempt. Six isolated local repetitions passed and are not treated as a correction.

A bounded 1.5-second delay of the initial Environment metadata response then reproduced a fixture race: Playwright's `setInputFiles` does not wait for enabled state, so it could dispatch while metadata was still loading; the guarded save correctly refused the premature request. The next saved-status assertion failed. The test now explicitly waits for the upload control to become enabled and retains the one-shot delayed response as a regression. Three corrected Chromium repetitions passed, with zero retries or skips (focused verification only). This mechanism is consistent with the CI symptom, not conclusive attribution of the original failure without its assertion location. No product guard or timeout was weakened.

The Firefox failure was subsequently reproduced in four of five bounded repetitions (an initial isolated pass was not treated as a fix). The keyboard helper waited for visibility, not enabled state, and exhausted its tab attempts while Resume still disabled controls. Waiting for enabled state and the authoritative resumed view corrected that fixture race. The corrected Firefox flow passed five repetitions; the helper regression and full accessibility flow then passed all six cases across Chromium, Firefox and WebKit. This is focused evidence pending the next complete clean-checkout run.

The Workflow test was strengthened to start the installed local carrier with generated production handlers in one child process, persist a human wait, kill that process with `SIGKILL`, and resume the same history in a different process. A deliberate 1.5-second fixture-model delay exercises bounded distributed polling beyond Vitest's default one-second expectation window. The resumed run issues no repeated model request (one total), rechecks domain Stop authority, and excludes protected configuration from history. Focused Workflow verification passed two tests with zero unhandled errors; the location-only reporter's privacy regression also passed. The earlier CI failure cannot be attributed conclusively because its assertion location was not retained. New CI artifacts contain allowlisted locations, never error bodies or history.
