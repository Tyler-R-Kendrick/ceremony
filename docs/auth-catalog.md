# Auth ceremony catalog

Reviewed 2026-09-08. Interaction families are distinct from protocol profiles. “Implemented” below means exercised against this project's local HTTP test service, not certified against all external providers.

The simulation page maps six families to GitHub, Stripe, Jira, Supabase and Neon. A seventh runtime family, `github-app`, is separately available in live mode: manifest registration → installation approval → signed server-side access verification. It is fixture-tested, not yet certified against an actual account. See [live authentication](live-auth.md). [Service examples](service-examples.md) records the simulation boundary. In addition to the original WorkOS email/code profile, `createNeonAdapter` implements Neon's provider-owned project transfer and reconciliation profile; these claim protocols are deliberately separate.

| Family / profile                                   | Actors and prerequisites                                                       | Ceremony and completion evidence                                                                                               | Failure / recovery                                                                         | v1                                                                                                                                                                                     |
| -------------------------------------------------- | ------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| API key / personal access token                    | User already has a credential; connector supplies field and validation adapter | Collect masked credential → provider validates → opaque connection reference                                                   | Invalid credential → retry; cancel clears fields                                           | Implemented                                                                                                                                                                            |
| HTTP Basic                                         | User credentials; HTTPS resource supports Basic                                | Username/password → authenticated resource request → validated connection                                                      | Reject invalid credentials and colon in username; retry                                    | Implemented; [RFC 7617](https://www.rfc-editor.org/rfc/rfc7617.html)                                                                                                                   |
| Forms/session auth                                 | Provider-specific sign-in endpoint and known schema                            | Declared fields → adapter sign-in → verified provider session/credential                                                       | Field errors or sign-in rejection → retry                                                  | Single-step reference endpoint implemented; no universal forms-auth standard or site scraping                                                                                          |
| OAuth authorization code + PKCE                    | Registered public client, callback, provider endpoints, permissions            | Consent review → provider → state-bound callback → code/verifier exchange → credential                                         | Denial, mismatched state/PKCE, replay, timeout → new attempt                               | Implemented; [RFC 6749](https://www.rfc-editor.org/rfc/rfc6749.html), [RFC 7636](https://www.rfc-editor.org/rfc/rfc7636.html), [RFC 9700](https://www.rfc-editor.org/rfc/rfc9700.html) |
| OAuth device authorization                         | Registered client and device endpoint; user can approve separately             | Request codes → show user code and URL → interval-bound polling → token                                                        | Pending, slow_down, denial, expiration; explicit restart                                   | Implemented; [RFC 8628](https://www.rfc-editor.org/rfc/rfc8628.html)                                                                                                                   |
| auth.md anonymous + claim                          | Service supports anonymous identity registration and claim grant               | Register → exchange signed assertion → anonymous access → optional email/approval code → claim token exchange → claimed access | Anonymous access and claim outcome tracked separately; retry claim or continue anonymously | Implemented documented profile; [WorkOS auth.md apps](https://workos.com/auth-md/docs/apps)                                                                                            |
| auth.md service_auth                               | Service requires user approval before granting access                          | Email hint → approval code/URL → claim polling → first credential                                                              | Denial, expiry, identity mismatch                                                          | Catalog only; same auth.md source                                                                                                                                                      |
| auth.md identity assertion                         | Trusted agent issuer, assertion and service registration policy                | Consent to assert identity → signed assertion → service registration/exchange                                                  | Untrusted issuer, invalid proof, reauthentication or step-up                               | Catalog only; same auth.md source                                                                                                                                                      |
| OpenID Connect                                     | OAuth provider plus OIDC discovery and ID-token requirements                   | Redirect → validated identity claims → login/session                                                                           | Nonce/issuer/audience failure, expired identity, account selection                         | Catalog only; [OIDC Core](https://openid.net/specs/openid-connect-core-1_0.html)                                                                                                       |
| SAML federation                                    | Service provider, identity provider, metadata and trust                        | Redirect/post binding → verified assertion → session                                                                           | Signature, audience, recipient, expiry or replay failure                                   | Catalog only; [OASIS SAML 2.0](https://docs.oasis-open.org/security/saml/v2.0/)                                                                                                        |
| Passkeys / WebAuthn                                | Relying party challenge, browser and authenticator                             | Browser credential ceremony → server validates proof                                                                           | Cancellation, unavailable authenticator, invalid origin/challenge                          | Catalog only; [WebAuthn](https://www.w3.org/TR/webauthn-3/)                                                                                                                            |
| OTP / magic link / MFA                             | Provider-issued challenge and delivery channel or authenticator                | Initiate challenge → enter code/follow link/provide additional factor → verify                                                 | Resend limits, expired/reused proof, wrong code, recovery                                  | Catalog only; provider-specific interaction, [TOTP RFC 6238](https://www.rfc-editor.org/rfc/rfc6238.html)                                                                              |
| Client credentials / JWT assertions                | Machine identity with server-side secret or signing key and explicit trust     | Select/configure identity → authenticated exchange → service access                                                            | Invalid client, signature/audience/expiry, key rotation                                    | Catalog only; [RFC 6749](https://www.rfc-editor.org/rfc/rfc6749.html), [RFC 7523](https://www.rfc-editor.org/rfc/rfc7523.html)                                                         |
| MCP HTTP authorization                             | Protected resource metadata, authorization server and client registration      | Discover → register/identify client → OAuth + PKCE with resource binding → access                                              | Missing discovery/PKCE, invalid resource, insufficient scopes/step-up                      | Catalog only; [MCP 2026-07-28](https://github.com/modelcontextprotocol/modelcontextprotocol/blob/main/docs/specification/2026-07-28/basic/authorization/index.mdx)                     |
| Public / no authentication                         | Resource explicitly permits unauthenticated use                                | Confirm choice → public access, without claiming an authenticated identity                                                     | Resource later requires authorization                                                      | Catalog only; distinct from anonymous registration that issues credentials                                                                                                             |
| Certificate / workload federation / token exchange | Configured workload trust and external signer/runtime                          | Select configured identity → proof or exchange → access                                                                        | Untrusted certificate/issuer, binding/expiry failure                                       | Catalog only; [OAuth mTLS](https://www.rfc-editor.org/rfc/rfc8705.html), [Token Exchange](https://www.rfc-editor.org/rfc/rfc8693.html)                                                 |

“Agent JWT” is not a universal flow: each connector must name its assertion/trust profile. PKCE is a protection within authorization-code flow. MCP is a protocol profile layered over OAuth; its discovery and resource requirements are not satisfied merely by rendering an OAuth button. MFA may be a step inside another ceremony. Deprecated OAuth implicit/password grants receive no implementation.

The auth.md adapter uses `urn:workos:agent-auth:grant-type:claim`, explicitly configured identity/claim endpoints, and service-signed JWT-bearer exchange. It does not infer endpoints from Markdown. Its local provider signs ES256 assertions and validates issuer, audience, type, and expiry. Anonymous tokens are invalidated when claiming completes, and the adapter replaces the connection's stored credential with the returned claimed credential.

The catalog can grow without adding speculative runtime APIs. Implement a new family only with its concrete adapter, screen requirements, failure semantics, and runnable protocol/browser checks.
