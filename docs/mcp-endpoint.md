# Driving ceremonies from a chat client

Until now an agent could drive a ceremony only through the browser application's HTTP API. The MCP endpoint gives a chat client the same four run operations, against the same runtime, under the same authorization — and the same recording, authoring and chaining operations the browser's teaching routes offer, except review and publication.

This page is about Ceremony **serving** MCP. Connecting outward to somebody else's MCP server is a different thing entirely, and the last section says why.

## One implementation, two transports

`src/server/agent-tools.ts` holds what an agent may do to a run: `connect`, `snapshot`, `advance`, `cancel`. The HTTP route and the MCP server both call it. `src/server/teaching-operations.ts` does the same for the teaching routes: the request schemas and service calls for demonstrations, drafts, published recipes and authored connectors, used by `teaching-http.ts` and by the MCP tools in `src/server/mcp-teaching.ts`. The authoring tools are registered from the same definitions (`authoringToolDefinitions` in `src/core/authoring-tools.ts`) the browser registers as WebMCP tools.

That sharing is the point rather than a tidiness preference. The rule these operations exist to enforce — a run may be driven only by the session that created it — is one comparison in one place. Two copies of an authorization rule is not duplication, it is a hole waiting for one copy to be updated.

## What the endpoint registers

Every tool below takes the actor from the host's `authenticate` and never from an argument, and the service behind it re-checks capability and ownership. Tools carry MCP `readOnlyHint` / `destructiveHint` annotations; those are hints for a client, not the authorization.

**Always registered:** `ceremony_connect`, `ceremony_snapshot`, `ceremony_advance`, `ceremony_cancel` and `ceremony_connectors`. The first four are the run operations above. `ceremony_connect` also takes an optional `teach: true`, which records the run as a demonstration from its first step (it needs the `author` capability and is refused before any run starts otherwise). `ceremony_connectors` lists the host's registered connectors **and the connectors this actor authored and installed** — the same list the browser's capabilities route reports — together with where credential entry happens.

**Registered by capability.** A tool the authenticated actor could never use is left out rather than offered and refused; the services still refuse regardless. `admin` counts as every capability, as it does everywhere else.

| Tools                                                                              | Offered to                        | What they do                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ---------------------------------------------------------------------------------- | --------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ceremony_author_from_provider`, `ceremony_author_compose`, `ceremony_author_read` | `author`                          | Draft a connector for a named provider from its published auth metadata, compose its ceremonies, read the draft. Same schemas and result allowlist as the WebMCP tools; nothing is executed and no credential is collected.                                                                                                                                                                                                                                                                                 |
| `ceremony_author_verification_propose`                                             | `author`                          | Propose how an authored API-key, Basic or form connector checks a collected credential (one HTTPS request to an origin its provider declared). Same schema and checks as `POST /authoring/installed/:id/credential-verification`; the result is `pending-review` with a digest, and it verifies nothing until a person approves that digest on the connector's page in the application (the Proposed credential check card, which posts to `…/credential-verification/approve`). There is no approval tool. |
| `ceremony_author_delete`                                                           | `author` and `executor`           | Delete a local authored connection and its stored session; the provider account is untouched.                                                                                                                                                                                                                                                                                                                                                                                                               |
| `ceremony_demonstration_start`, `ceremony_demonstration_consent`                   | `author`                          | Start recording a run you own; pause, resume, stop (`stopped`) or discard it. Recorded events are the public projection the application shows, never values a person entered.                                                                                                                                                                                                                                                                                                                               |
| `ceremony_demonstration_read`                                                      | `author`, `reviewer`              | Read a recording's events by sequence number.                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `ceremony_draft_compile`, `ceremony_draft_import`, `ceremony_draft_edit`           | `author`                          | Compile a span of a recording into a recipe draft, import a draft from JSON, or replace a draft's definition.                                                                                                                                                                                                                                                                                                                                                                                               |
| `ceremony_draft_read`                                                              | `author`, `reviewer`, `publisher` | Read a draft, its digest and its diagnostics.                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `ceremony_recipes`, `ceremony_recipe_preview`                                      | `executor`                        | List published recipes with the version and digest that pin them; check a definition without saving or executing it.                                                                                                                                                                                                                                                                                                                                                                                        |
| `ceremony_recipe_compose`                                                          | `author` and `executor`           | Compose two or more published recipes into a new **draft**.                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `ceremony_recipe_execute`                                                          | `executor`                        | Execute a **published** recipe pinned by id, version and digest, binding only public inputs — the same checks as `POST /recipes/execute`. The run is then delegated to this session under host policy, as `ceremony_connect`'s is, so `ceremony_advance` can drive it; `delegated: false` means host policy refused that and a person continues it in the application.                                                                                                                                      |

**Review and publication are human-only.** There is no tool to review, publish or retire a recipe, for any actor. An agent can record, draft, compose and preview, but it can execute only what a person has already reviewed and published, pinned by digest; a draft is never executable.

**Registered when the host opts in:**

- `ceremony_agent_start` — only when the runtime has a model configured (without one the start route answers `unavailable` every time, so the tool is not offered). It starts the server's own agent on one of the caller's runs, through the same implementation as `POST /api/v1/teaching/agent/:runId/start` (`startAgent` in `src/server/agent-tools.ts`): `executor`, a run the caller can read and has not cancelled or stopped, and the host's `authorize` policy for handing the run to the agent, the roles a deployment configures. It answers exactly as the route does: `{ turnId, status: "running" }` when the host passed a launcher (`startAgent` in `createCeremonyMcpHandler`'s options; the hosted deployment passes the same durable workflow as its HTTP route), or the turn's outcome when it ran inline, with the same `handoff` the status route projects while the turn waits on a person or is uncertain. Stopping the agent and reading its status stay on the HTTP routes.
- `connector_catalog`, `connector_status`, `connector_connect` and `connector_invoke` — only when the host passes the optional `connectors` option to `createCeremonyMcpHandler`. They are a transport over the connector command service, which re-checks authorization itself; the tools enforce projection and output policy. No result carries a credential, a code, a provider URL or a destination. `connector_invoke` returns output when the binding classified it public, or personal where a person gave `agentOutputConsent: "personal"` when approving the binding (below); a secret result is always withheld from this transport and stays readable by the person in the application.
- `connector_verify` and `connector_revoke_request` — when the `connectors` dependencies also supply `verify` and `requestRevocation` (over `ConnectorCommandService.verify` and `.requestRevocation`). A tool is not offered when its dependency is absent. `connector_verify` asks the provider for fresh evidence about an existing grant and never obtains a new one. For an outbound MCP server connection under the default OAuth profile, an expired access token with a held refresh token is renewed once before that evidence is read, as `connector_invoke` and a resumed input request already do; only a renewal the issuer refuses leaves the connection `reconnect-required` (outcome `mcp.credential-renewal-failed`). An unreachable issuer is `mcp.credential-renewal-unavailable` and changes nothing, so a brief outage does not send a person to reconnect; a lost or raced refresh is `indeterminate` (`mcp.credential-renewal-indeterminate`). `connector_invoke` classifies its own renewal the same way: `denied` only for a refusal, `failed` with `mcp.credential-renewal-unavailable` for an unreachable issuer, and `indeterminate` for an unknown outcome; the operation itself did not run in any of them. Nothing the issuer said reaches the result. `connector_revoke_request` revokes nothing: it records a pending request on the connection (its last outcome reads `revoke.requested`) and returns `revocation: "pending-approval"` with the owner's page path. An administrator approves by revoking, as a person, through the existing admin-only `POST /api/v1/connectors/connections/:ref/revoke`; an administrator declines, also as a person and under the same policy, with `…/revoke-decline`. There is no path by which an assistant revokes access itself.
- Every `connector_*` tool carries MCP annotations (`readOnlyHint`, `destructiveHint`, `idempotentHint`, `openWorldHint`). They are hints for the client; the service still decides.
- The connector intents — `connector_list`, `connector_inspect`, `connector_status`, `connector_connect`, `connector_operations`, `connector_reconnect` and `connector_disconnect` — only when the host passes `connectorIntents` (the reference application does, when it has a connector runtime). Every result goes through the agent projection: no credential, destination or provider link; a waiting handoff gains only the owner's page `path` described below. When `connectors` is also passed, the intents skip the two names those tools already hold (`connector_status`, `connector_connect`); on their own, they register all seven.
- `browser_login`, `browser_session_status`, `browser_release` and `browser_backends` — only when the runtime was built with a browser executor (`browserLogin`). Credentials are passed as collector references, never values, and `browser_release` does not log an account out at the provider. `browser_login` also accepts a published [recorded ceremony](recorded-ceremonies.md) in `draft.recording`, pinned by id, version and digest, and replays it with no model.
- `browser_record_login` and `ceremony_recording_read` — alongside the browser tools, and only for an actor holding `author`. The first drives a login with recording on and saves what worked as a value-free **draft**. It needs `executor` too. The second reads a draft or a published version. Nothing reviews or publishes a recording over MCP; those are human-only HTTP routes.
- `ceremony_collect_private` and `ceremony_bind_private` (the MCP App collector) — only when both origins are HTTPS; see below.
- `ceremony_arazzo_import` — offered to `author`, and only when the runtime was built with `arazzoCatalog`, the host's reviewed Arazzo operation-binding catalog. It compiles one workflow of an Arazzo description, given as JSON text, into a recipe **draft**, or returns the blocking issues (codes, pointers and fixed messages) and no draft. The same operation is `POST /api/v1/teaching/drafts/arazzo`. Source URLs are never fetched, and every step must map to an operation the registry already holds. The draft runs only after a person reviews and publishes it. See [Arazzo profiles](specifications/arazzo-profiles.md).

### Handoffs an assistant can pass on

When a connector tool result waits on a person (`handoff.state` is `issued` or `waiting`), the handoff carries `path`: the same-origin path of the owner's page for that connection, `/connectors?connection=<connectionRef>` by default (`humanRoute` in the tool context changes the mount). The connector intents (`connector_list`, `connector_status`, `connector_connect`, `connector_reconnect`) add the same `path` to the same handoffs, so reconnecting through an intent is not a dead end either. The path holds only the connection reference the assistant already has: no token, code, `state`, device code or provider URL. Opening it still requires the owner's own session, so passing it on grants nothing. The provider page, device code or private form is shown to that person on that page, through the human projection. A handoff that is no longer waiting has no path.

Ceremony runs work the same way. When a node of a run waits on a person, `ceremony_connect`, `ceremony_snapshot` and `ceremony_advance` return the run with a `handoff`: the server agent's descriptor (`src/server/agent/coordinator.ts`) with `kind: "person"`, the run, node and operation ids, the node state, a fixed `reason`, and, for a node that is `awaiting-human`, the `path` of the run's authenticated human route (`/api/v1/teaching/<provider>/<runId>/human`). A node that is only verifying or uncertain has a handoff without a path, because there is no page to open. The agent's HTTP start route, and `ceremony_agent_start`, return the same descriptor as its status route when the turn runs inline.

### Personal output and owner consent

A binding's reviewer can approve `agentOutputConsent: "personal"` alongside the binding's operations. Only a human actor can set it; an assistant with reviewer capability is refused (`consent.human-only`). The consent becomes part of the binding revision and its reviewed digest, so a connection keeps the consent its binding was approved with. The default connector policy then lets an assistant invoke operations classified personal on that binding and read their output, and the result says `agentOutputConsent: "personal"`. Secret-classified output has no consent: the service withholds it from an assistant whatever a custom policy returns, and the MCP tool withholds it again.

## How often a client may call

Every tool call is budgeted per actor and per tool (`src/server/mcp-rate-limit.ts`): each (tenant, subject, tool) has a token bucket of 30 calls that refills at one call every two seconds. It is keyed on the subject, not the session, so a person's several chat clients share one allowance. A call over budget is not run; it answers a tool error whose structured content is `{ "error": "rate-limited", "tool": "<name>", "retryAfterSeconds": <n> }` (the text content carries the same JSON plus a sentence), so a model can wait rather than retry blindly. The budget is enforced where tools are registered, so it covers every tool the endpoint offers, including the connector tools, the intents and the collector's app tools.

A host sets it with `rateLimit` on `createCeremonyMcpHandler`: `capacity` and `refillPerSecond` for every tool, `tools` for per-tool overrides by name, `now` for an injected clock, and `maxBuckets` (default 10 000; the least recently used bucket is forgotten first). `rateLimit: false` turns it off for a host that throttles in front of the endpoint. The buckets live in the process: this paces a model, it is not a durable quota, and a restart or a second instance starts fresh. The browser routes' store-backed `reserveRequest` budgets and the connector service's own limits are unchanged, and still apply behind the tools that reach them. The hosted endpoint uses the defaults.

## What authenticates a chat client

The browser is authenticated by a cookie over an OIDC code flow. A chat client is authenticated by an access token, validated against the issuer's published keys and required to name this MCP endpoint as its audience. Both end at the same `ActorContext`, so capabilities, ownership and delegation are enforced once.

The audience check is what separates a token minted for this deployment from any other token the same person holds from the same issuer. Without it, every token that issuer ever signed would drive these ceremonies.

`/.well-known/oauth-protected-resource/mcp` publishes only what [RFC 9728](https://datatracker.ietf.org/doc/html/rfc9728) asks of a resource server: which authorization server issues its tokens. The client reads the authorization server's own metadata from the issuer, so nothing here restates it and nothing can drift out of date with it. A request with no token, or one the host refuses, is answered `401` with `WWW-Authenticate: Bearer resource_metadata="…"`, which is how a client discovers where to go.

## Why the endpoint sits outside the browser boundary

Every request to the browser application passes `assertRequestBoundary`, which requires an `Origin` header equal to the application's own. That is a CSRF defence, and it is a defence specifically for requests authenticated by an ambient cookie — the browser attaches those whether or not the page meant to.

A chat client is not a browser. It sends no `Origin`, and it carries a bearer token that a hostile page has no way to cause to be attached. Applying the check would reject every MCP request while defending against nothing, so the MCP handler is mounted ahead of it. It authenticates every request itself and answers only its own two paths, returning `undefined` for everything else.

## A session, and why it is not the token

A run may be driven only from the session that created it. So the session cannot be the token's own identifier: refreshing an access token in the middle of a ceremony would orphan the run the previous token started, and the person would be told their own run belongs to someone else.

Subject plus client id is stable across refresh, and still separates two chat clients held by the same person. Reading and driving are deliberately different permissions: the same person in a second session can read a run and cannot drive it.

## Credentials still never cross MCP

The tools carry run state. They do not carry credentials, and there is no tool that accepts one.

When a ceremony needs a value from a person, the MCP App collector renders in the chat client and posts the value **directly to the credential broker over HTTPS**. The assistant receives a one-use reference; the value is never in a tool argument, a tool result, or the conversation. `ceremony_bind_private` binds that reference and, by its own description, never accepts a raw credential.

That collector requires stable HTTPS origins for both the broker and the app. On a plain-HTTP development origin it is **not registered at all**, rather than registered in some weaker form — `ceremony_connectors` reports `privateCollection: "web-application-only"` so a client can tell the difference, and credential entry stays in the authenticated web application. A test asserts the collector stays unmounted on an HTTP origin.

## Configuration

The endpoint is built from the same protected environment as the web application, so a deployment cannot offer chat access under a different issuer or tenant than the application beside it. It needs `CEREMONY_PUBLIC_ORIGIN`, `CEREMONY_OIDC_ISSUER` and the hosted tenancy (`CEREMONY_TENANT_ID`, `CEREMONY_TENANT_CLAIM` or both), all of which the hosted server already requires. Tenant and capabilities are mapped from the access token's signed claims by the same object the browser identity uses (see [host identity](host-identity.md)), so a token without a configured tenant claim, or with a malformed roles claim, is refused.

The hosted endpoint passes both `connectors` and `connectorIntents` when its connector runtime is on (the default; `CEREMONY_CONNECTORS=disabled` turns it off). There, `connector_connect` takes a connector id and resolves it against the tenant's approved bindings: a binding reference names itself, and an adapter id names its binding only when exactly one is approved. Anything else is refused rather than guessed.

Absent or invalid configuration produces no handler rather than an open one: the route answers `503` and no ceremony is reachable over MCP at all.

## What this does not do

It does not register an OAuth client for you, and it is not an authorization server. It validates tokens; it does not issue them. Dynamic client registration, consent, and token issuance belong to the configured issuer.

It does not certify any provider. Driving a real provider from chat still runs the same ceremonies with the same evidence requirements as the browser, and the same human steps still cost a person's attention.

## Inbound is not outbound

Two unrelated things in this repository both say "MCP", and confusing them is how a deployment ends up forwarding the wrong token to the wrong party.

|                        | **Inbound: Ceremony as an MCP server**                                         | **Outbound: Ceremony as an MCP client**                                               |
| ---------------------- | ------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------- |
| Code                   | `src/server/mcp.ts`, `src/server/agent-tools.ts`                               | `src/server/connectors/mcp/`                                                          |
| Who authenticates whom | A chat client presents a bearer token **to** this deployment                   | This deployment presents a credential **to** somebody else's server                   |
| The token              | Issued by this deployment's configured issuer, audience-bound to this endpoint | Issued by the remote server's authorization server, held in this deployment's custody |
| Discovery              | This endpoint **publishes** RFC 9728 protected-resource metadata               | The client **reads** the remote server's 401 challenge and metadata                   |
| Client registration    | Not our concern; the configured issuer owns it                                 | Pre-registered, or CIMD, or Dynamic Client Registration, in host policy order         |
| Protocol revision      | Whatever `@modelcontextprotocol/server` v2 serves                              | Pinned per binding: `2026-07-28`, or the `2025-11-25`/`2025-06-18` legacy era         |
| What a failure means   | A client cannot drive a run here                                               | A connection to an external service is unavailable                                    |

The rule that matters: **a Ceremony token is never passed to an unrelated downstream service, and a remote server's credential never authenticates a request to this endpoint.** The outbound client validates the resource and audience of what it obtains, and an access token valid for another resource is not accepted or forwarded as authority. The two sides share no credential, no session and no client registration.

The outbound client has its own boundaries, documented in the [dialect profile](specifications/connector-dialects.md#the-mcp-client-subset): sampling is refused rather than suspended, roots answer an empty list, extensions are never assumed, stdio transports are unsupported by design because a hosted connector launches no local process, and no live or vendor-certified evidence exists for any of it.
