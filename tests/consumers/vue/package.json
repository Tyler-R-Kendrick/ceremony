{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit --strict --module ESNext --moduleResolution Bundler --target ES2024 --skipLibCheck main.ts && vite build --target esnext"
  },
  "dependencies": {
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vite": "^7.1.0"
  }
}
