asyncapi: 3.0.0
info:
  title: Events Fixture
  version: 0.9.0
servers:
  production:
    host: events.example:8883
    protocol: mqtt
channels:
  orders:
    address: orders/created
operations:
  onOrder:
    action: receive
    channel:
      $ref: "#/channels/orders"
