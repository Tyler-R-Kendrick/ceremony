openapi: 3.1.0
info:
  title: Refresh
  version: "1.0.1"
  description: First revision, with a clarified description.
servers:
  - url: https://api.example/v1
components:
  securitySchemes:
    oauth:
      type: oauth2
      flows:
        authorizationCode:
          authorizationUrl: https://auth.example/authorize
          tokenUrl: https://auth.example/token
          scopes:
            read: Read
paths:
  /items:
    get:
      operationId: listItems
      summary: Lists items in the caller's workspace.
      parameters:
        - name: filter
          in: query
          schema: { type: string }
      responses:
        "200":
          description: ok
