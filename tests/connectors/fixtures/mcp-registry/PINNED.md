# Pinned MCP registry artifacts

Fetched 2026-09-18 from the primary sources below. Tests never reach the network; the loopback double in `tests/connectors/doubles/mcp-registry.ts` is written from these documents, not from the client under test.

| Artifact                        | Source                                                                                                                                                                                                                             | Pin                                                                                                                                                                                                                                                                                 |
| ------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `server.schema.2025-12-11.json` | <https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json>                                                                                                                                                     | upstream bytes sha256 `3fba09590c99f61735d234822279f4223fab9e300c0a81e81c91ab62a4114de0`; the copy here is the same JSON re-indented by this repository's formatter (sha256 `8ca42989ecdbce74f5bd0301f3db4cc760e6ad8d1054d44734a24ed0f977f998`, identical when parsed)              |
| REST API                        | <https://registry.modelcontextprotocol.io/openapi.yaml> (`Official MCP Registry`, info.version 1.0.0, OpenAPI 3.1.0) and the generic spec `$id` <https://modelcontextprotocol.io/schemas/draft/2025-12-01/server-registry-openapi> | `/v0.1`: `GET /servers` (`cursor`, `limit` 1-100 default 30, `search`, `updated_since`, `version`, `include_deleted`), `GET /servers/{serverName}/versions`, `GET /servers/{serverName}/versions/{version}` with the `latest` alias, `POST /publish` (bearer registry JWT)          |
| Aggregator guidance             | <https://modelcontextprotocol.io/registry/registry-aggregators>                                                                                                                                                                    | base URL `https://registry.modelcontextprotocol.io`; path parameters URL-encoded exactly once (`io.modelcontextprotocol%2Feverything`); `metadata.nextCursor`; `status` may move to `deprecated` or `deleted`; scrape hourly and persist locally; no uptime or durability guarantee |
| Registry overview               | <https://modelcontextprotocol.io/registry/about>                                                                                                                                                                                   | preview status; reverse-DNS namespaces; the registry authenticates namespaces and hosts metadata only — it does not host code and delegates security scanning                                                                                                                       |

`servers/*.json` are the documented examples from `docs/reference/server-json/generic-server-json.md` in `modelcontextprotocol/registry` (`brave-search`, `filesystem`, `hybrid`, `mcpb`, `remote-templated`, `embedded-no-runtime`) plus three adversarial documents authored here: `malicious-package.json` (AC-MCP-08), `legacy-snake-case.json` (pre-2025-09-16 field names) and `missing-schema.json` (no `$schema`). `api/servers-page.json` is a list page in the `ServerListResponse` shape with active, deprecated and deleted `_meta` entries.
