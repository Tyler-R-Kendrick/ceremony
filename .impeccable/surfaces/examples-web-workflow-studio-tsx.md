---
version: 1
slug: "examples-web-workflow-studio-tsx"
primary_target: "examples/web/workflow-studio.tsx"
related_targets: ["examples/web/style.css"]
---

## Direction contract

THESIS: Studio authors a new connector and its ceremonies. It never opens an existing connection or reads session Environment data. Operate mode.

OWN-WORLD: Inherit the daylight workbench, native form controls, system typography and cobalt actions. No replacement visual identity.

STORY: Name a connector, define authentication methods, order SDK-backed steps and human fallbacks, then validate and export a reusable definition. Drafts carry procedure, never credentials or authority.

FIRST VIEWPORT: Connector identity and an obvious create action lead. The editor progresses through connector, methods and review, with a plain-language summary. Mobile follows the same order. Signature interaction: editing steps changes the exported Arazzo workflow, not a live service.

FORM: User-specified authoring correction; code-led extension of the established workbench (inherited seed 8302fddc). Preserve Connect and Environment behavior.

FINISH: unreviewed and undocumented is unfinished; this build ends with the finish review, the verdict, DESIGN.md, and every shipping raster carrying its provenance
