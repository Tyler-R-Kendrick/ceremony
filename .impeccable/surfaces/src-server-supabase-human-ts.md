---
version: 1
slug: "src-server-supabase-human-ts"
primary_target: "src/server/supabase-human.ts"
related_targets: ["src/react/teaching.tsx"]
---

## Direction contract

THESIS: Finish the current Supabase project-user connection from its first missing prerequisite. Operate mode; avoid a separate setup product or protocol menu.

OWN-WORLD: Inherit the daylight workbench, cobalt controls, system typography, flat white surface, native forms and visible focus. The private page remains isolated from the app shell and assistant.

STORY: Resolve project configuration, offer existing-user sign-in or explicit signup, explain email confirmation, and request an enrolled authenticator only when required. Dashboard setup is distinct from project-user access; provider evidence alone completes the connection.

FIRST VIEWPORT: One heading, concise current-blocker instructions, one native form and a return link. Project setup links precede configuration fields. Secondary signup/sign-in choices stay within the same form. Mobile keeps the same order with 44px controls and no horizontal overflow.

FORM: Narrow code-led extension of the existing workbench; inherited seed 8302fddc, no new concept round. Signature interaction: successful private submission advances the same parent and presents only the next blocker. No decorative motion.

FINISH: unreviewed and undocumented is unfinished; this build ends with the finish review, the verdict, DESIGN.md, and every shipping raster carrying its provenance

## Constraints

No capture of collector/provider/Environment screens or request content. Use safe empty Connect images only, with native DOM and HTTP assertions for protected forms. No schema editor, narration, new brand or new raster assets. User has already required implementation without ordinary approval checkpoints.

## Outcome — 2026-09-10

The narrow code-led extension inherits seed `8302fddc`. The private native collector covers missing project configuration, existing-user sign-in or explicit signup, email confirmation and an enrolled authenticator when required. Supabase operation labels and the uncertain-state continuation link reuse the existing Connect presentation. The collector advances the same parent connection to its next blocker; verified provider evidence remains the completion boundary.

The finish reviewer returned **ship**, scoring all three requested fixes resolved: safe wrong-password/MFA rejection notices, actionable in-place invalid-project correction, and browser rejection/recovery assertions. The implementation handoff reports six passing local browser cases (AAL1 and AAL2 in Chromium, Firefox and WebKit), using mounted handlers, PostgreSQL, the SDK and signed local HTTP. This is local verification, not live Supabase certification or an overall production/readiness claim. The single UI detector pass returned `[]`.

Documentation evidence checked: `PRODUCT.md`, `DESIGN.md`, `.impeccable/design.json`, this direction contract, `src/server/supabase-human.ts`, Supabase labels and recovery rendering in `src/react/teaching.tsx`, `src/react/teaching.css`, and `tests/browser/teaching-supabase.spec.ts`. The browser source asserts invalid-key correction on the same private URL, wrong-password feedback, rejected-code feedback with cleared input, successful recovery, and return to the same parent. No tests or servers were rerun for this documentation pass.

The two inspected safe empty Connect captures are `test-results/teaching-supabase-Supabase-b4103-up-and-private-verification-chromium/supabase-connect-1440.png` and `test-results/teaching-supabase-Supabase-b4103-up-and-private-verification-chromium/supabase-connect-390.png`. They originate from the Chromium AAL1 browser case before opening the private collector and document desktop/mobile Connect only. Collector, provider and Environment captures remain prohibited; protected states were assessed through source and assertions, without private screenshots. No comps or new raster assets were introduced.

The extension retains the cool canvas, ink text, cobalt actions and white surfaces; system typography and native 16px inputs; 6px controls with 44px minimum heights; visible cobalt focus; and a flat layout without decorative motion. The collector's 680px width and 720px stacking threshold are local composition choices, not new global tokens. `DESIGN.md` and `.impeccable/design.json` are preserved because no global system change was approved.

Pre-existing drift was not repaired or canonized: the sidecar still describes “Template studio” and “template authoring”, whereas the current design document and safe captures use Workflow studio/connector authoring. This ordinary extension does not authorize a system refresh.
