---
version: 1
slug: "examples-web-main-tsx"
primary_target: "examples/web/main.tsx"
related_targets: ["examples/web/style.css"]
---

## Direction contract

THESIS: Make the actual authentication interaction the first viewport, replacing the marketing-led cream-and-serif shell. Visitor mode: Operate; developers and end users need an obvious next action.

OWN-WORLD: A daylight integration workbench, cool off-white canvas, ink text, cobalt selection, white working surfaces, native controls, compact system typography and clear dividing rules. No decorative illustration or gauge metaphors.

STORY: Choose a commonplace service and review its real authentication family. Live GitHub is separate from simulations: prepare or reuse the app, approve its installation, then verify provider access. Each prerequisite blocks downstream work. Template studio stays one navigation step away.

FIRST VIEWPORT: Compact header, short page heading, service rail on the left, dominant ceremony in the center and a quiet current-state context column on the right. Primary action appears without scrolling at ordinary desktop sizes. Mobile stacks in task order. Signature interaction: selecting a method updates the form and context from the same client snapshot.

FORM: Request workbench, third grounded candidate, seed 8302fddc. Other grounded families considered: permission-review sheet, integration directory, installation assistant, security checklist, API reference spread, execution timeline. Proceeding with the proposed workbench while the user's asynchronous direction choice remains open; the choice is not recorded as explicit approval. Code-led implementation in the live workspace, not an approved-comp reproduction.

FINISH: unreviewed and undocumented is unfinished; this build ends with the finish review, the verdict, DESIGN.md, and every shipping raster carrying its provenance

## Approved extension — 2026-09-08

The user confirmed the orchestration brief in docs/ceremony-orchestration-design.md. Preserve this workbench world. Add an ordered, accessible prerequisite summary above the current action; native provider navigation and private collection are human fallbacks. Live and simulation modes must remain visually explicit. No new visual-world seed or comp is needed for this extension.

## Environment extension — 2026-09-08

Requested by the user: an Environment section with optional .env import and editing variables available to ceremonies. Preserve the incumbent Operate workbench. A single-column private configuration surface uses native connector selection, file upload, masked replacement fields and a named-variable list. Keep GitHub setup details in a disclosure; primary save and explicit removal remain clear. Values are server-only after entry and do not appear in saved rows or model tools. Required captures: environment-desktop.png (1440×1000), environment-mobile.png (390×844), under .impeccable/review. No new visual world or system rewrite.
