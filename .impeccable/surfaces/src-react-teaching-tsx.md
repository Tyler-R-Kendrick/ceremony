---
version: 1
slug: "src-react-teaching-tsx"
primary_target: "src/react/teaching.tsx"
related_targets: ["src/server/stripe-human.ts"]
---

## Direction contract

THESIS: Connect a service from its first missing prerequisite, not from a credential field. Operate mode; keep the current connection primary and teaching secondary.

OWN-WORLD: Inherit Ceremony's daylight workbench, cobalt controls, system text, native forms, visible focus, and flat surfaces. No visual identity change.

STORY: A new Stripe user opens provider-owned signup, returns to the same connection, creates a restricted key, and enters it in an isolated private collector. Existing configured access skips unnecessary human setup. Account readiness is a setup choice; only provider verification completes access.

FIRST VIEWPORT: Current blocker and one next action occupy the existing connection surface. The private page contains one short instruction group and a native form, no assistant, recorder, analytics, or general app shell. Desktop and mobile share task order; controls remain reachable at 390px.

FORM: Precisely specified extension of the existing code-led workbench; inherited seed 8302fddc. No new concept or comp round. Signature interaction: completion returns to and advances the same parent ceremony.

FINISH: unreviewed and undocumented is unfinished; this build ends with the finish review, the verdict, DESIGN.md, and every shipping raster carrying its provenance

## Verified extension outcome

The Stripe extension retains the documented daylight workbench: cobalt primary action, system typography, flat bordered card, and native controls. Connect precedes the quieter teaching action; at 390px the full Connect button remains within the initial viewport. Existing DESIGN.md and its sidecar remain the incumbent authority.

Finish review verdict: ship for the locally verified slice. The browser fixture covers account setup, ticket renewal after a long signup, native rejection of publishable keys, private key submission, and return/reload of the same verified parent. Three browser fixture runs passed as reported by the implementation handoff; this is not real-provider certification.

Visual evidence was inspected at 1440px and 390px. Both captures are generated by `tests/browser/teaching-stripe.spec.ts` in Chromium against the local Stripe fixture before connection begins:

- `test-results/teaching-stripe-Stripe-con-41c40-ds-the-same-verified-parent-chromium/stripe-connect-1440.png`
- `test-results/teaching-stripe-Stripe-con-41c40-ds-the-same-verified-parent-chromium/stripe-connect-390.png`

These are safe empty Connect captures, not provider or private collector screenshots. The test disables automatic screenshots, traces, and video; no sensitive-page capture is evidence for this review.
